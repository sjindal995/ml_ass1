function theta = unweightLR( file_1, file2 )
%UNTITLED5 Summary of this function goes here
%   Detailed explanation goes here
    x = importdata(file1);
    y = importdata(file2);
    [m,n] = size(x);
    x = [ones(m,n) x];
    n = n+1;
    theta = inv(x'*x)*x'*y;
    plot(x(:,2),x*theta,x
end

